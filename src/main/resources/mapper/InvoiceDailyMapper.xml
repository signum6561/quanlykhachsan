<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${ibatis.dao}.InvoiceDailyDAO">
    <resultMap 
        id="invoiceDailyRM" 
        type="InvoiceDaily" 
        extends="${ibatis.dao}.InvoiceDAO.invoiceDailyRM" 
    />

    <select 
        id="selectAll" 
        resultMap="invoiceDailyRM" 
        timeout="60000"
    >
        select i.*, d.rental_days
        from invoice i
        join invoice_daily d 
        on i.id = d.invoice_id
    </select>

    <insert id="insert" parameterType="InvoiceDaily">
        insert into invoice_daily(invoice_id, rental_days) 
        values(#{id}, #{rentalDays});
    </insert>

    <update id="update" parameterType="InvoiceDaily">
        update invoice_daily
        set rental_days = #{rentalDays}
        where invoice_id = #{id}
    </update>
</mapper>